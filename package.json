{
  "name": "ss-orca-ui",
  "theme": "ui",
  "version": "1.0.0",
  "description": "ss-orca-ui based on the Atom One dark UI theme",
  "keywords": [
    "dark",
    "adaptive",
    "ui"
  ],
  "license": "MIT",
  "author": "Nate Geslin",
  "repository": "https://github.com/n8rzz/ss-orca-ui",
  "bugs": "https://github.com/n8rzz/ss-orca-ui/issues",
  "main": "lib/main",
  "engines": {
    "atom": ">=1.13.0"
  },
  "devDependencies": {
    "coffeelint": "^1.9.7"
  },
  "configSchema": {
    "fontSize": {
      "title": "Font Size",
      "description": "Change the UI font size. Needs to be between 8 and 20.",
      "type": [
        "integer",
        "string"
      ],
      "minimum": 8,
      "maximum": 20,
      "default": "Auto"
    },
    "layoutMode": {
      "title": "Layout Mode",
      "description": "In Auto mode, the UI will automatically adapt based on the window size.",
      "type": "string",
      "default": "Auto",
      "enum": [
        "Compact",
        "Auto",
        "Spacious"
      ]
    }
  }
}
